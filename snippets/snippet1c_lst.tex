\begin{lstlisting}[title=\href{https://godbolt.org/z/ybbqrK}{\texttt{godbolt.org/z/ybbqrK}}]
// -O0
void side_effect();

int f(const int &x) {
    auto a = x;
    side_effect();
    return a - x;
}
\end{lstlisting}
