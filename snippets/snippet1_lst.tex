\begin{lstlisting}[title=\href{https://godbolt.org/z/E2HAfB}{\texttt{godbolt.org/z/E2HAfB}}]
void side_effect();

int f(int x) {
    auto a = x;
    side_effect();
    return a - x;
}

int g(const int &x) {
    auto a = x;
    side_effect();
    return a - x;
}
\end{lstlisting}
