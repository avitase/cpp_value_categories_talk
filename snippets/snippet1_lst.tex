\begin{lstlisting}[title=\href{https://godbolt.org/z/SPGBFL}{\texttt{godbolt.org/z/SPGBFL}}]
void side_effect();

int f(int x) {
    auto a = x;
    side_effect();
    return a - x;
}

int g(const int &x) {
    auto a = x;
    side_effect();
    return a - x;
}
\end{lstlisting}
