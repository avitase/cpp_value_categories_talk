\begin{lstlisting}[title=\href{https://godbolt.org/z/7j4zP7}{\texttt{godbolt.org/z/7j4zP7}}]
void side_effect();

int f(int x) {
    auto a = x;
    side_effect();
    return a - x;
}

int g(const int &x) {
    auto a = x;
    side_effect();
    return a - x;
}
\end{lstlisting}
