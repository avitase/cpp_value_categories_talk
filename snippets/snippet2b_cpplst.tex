\begin{lstlisting}[title=\href{https://godbolt.org/z/gJqDbA}{\texttt{godbolt.org/z/gJqDbA}}]
void side_effect();

int f(const int& x) {
    auto a = x;
    side_effect();
    return a - x;
}
\end{lstlisting}
