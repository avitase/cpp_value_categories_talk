\begin{lstlisting}[title=\href{https://godbolt.org/z/gzJQdi}{\texttt{godbolt.org/z/gzJQdi}}]
void side_effect();

int f(const int &x) {
    auto a = x;
    side_effect();
    return a - x;
}
\end{lstlisting}
