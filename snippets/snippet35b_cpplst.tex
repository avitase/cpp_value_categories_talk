\begin{lstlisting}[title=\href{https://godbolt.org/z/7Worb3}{\texttt{godbolt.org/z/7Worb3}}]
#include <iostream>
#include <type_traits>

struct S {
    S() { std::cout << 'a'; }
    S(const S&) { std::cout << 'b'; }
    S(S&&) { std::cout << 'c'; }
};

template <typename T>
S f(T&& t) { return std::forward<T>(t); }

int main() {
    S s{};
    f(s);
    f(std::move(s));
}
\end{lstlisting}
