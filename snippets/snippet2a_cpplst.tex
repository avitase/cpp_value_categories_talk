\begin{lstlisting}[title=\href{https://godbolt.org/z/m26mJX}{\texttt{godbolt.org/z/m26mJX}}]
void side_effect();

int f(const int& x) {
    auto a = x;
    side_effect();
    return a - x;
}
\end{lstlisting}
